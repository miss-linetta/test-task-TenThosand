import { PALETTE } from './palette';
import { FONT_SIZES, FONTS } from './typography';
import { Theme } from './types';

export const theme: Theme = {
  palette: PALETTE,
  typography: {
    fonts: FONTS,
    fontSizes: FONT_SIZES,
  },
};
